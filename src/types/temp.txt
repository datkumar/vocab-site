import "next-auth";

declare module "next-auth" {
  // Extending the built-in "User" type
  interface User {
    id: string;
    username: string;
    // more custom fields if needed
  }

  // Extending the "Session" type
  interface Session {
    user: {
      id: string;
      username: string;
      // more custom fields if needed
    };
  }
}

// Extending JWT token type
declare module "next-auth/jwt" {
  interface JWT {
    username: string;
    // more custom fields if needed
  }
}
